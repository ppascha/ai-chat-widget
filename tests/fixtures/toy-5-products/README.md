# toy-5-products fixture

This fixture provisions a deterministic toy WordPress storefront used by the chat plugin demos.

Contract:
- `seed.sql` contains SQL-level site settings.
- `seed.php` contains WordPress-level content seeding logic.
- `frontend-homepage.html` is the front-page content source.
